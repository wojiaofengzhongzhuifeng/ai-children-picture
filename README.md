# AI 图片生成 SaaS 应用

基于 Next.js 14、Supabase 和智谱 AI 的文本生成图片应用。

## 功能特性

- 🎨 文本生成图片（支持智谱 CogView-3 模型）
- 👤 用户认证系统（Supabase Auth）
- 🖼️ 图片广场（公开图片展示）
- 👤 个人中心（管理自己的图片）
- 🔧 管理后台（用户和图片管理）
- 📱 响应式设计（支持移动端）

## 技术栈

- **前端框架**: Next.js 14 (App Router)
- **样式**: Tailwind CSS
- **用户端 UI**: shadcn/ui
- **管理端 UI**: Ant Design 5
- **数据库**: Supabase (PostgreSQL)
- **认证**: Supabase Auth
- **AI 模型**: 智谱 CogView-3
- **状态管理**: Zustand

## 快速开始

### 📋 详细配置指南

请查看 [docs/setup.md](./docs/setup.md) 获取详细的配置和启动说明。

### 简要步骤

1. **安装依赖**
   ```bash
   pnpm install
   ```

2. **配置环境变量**
   ```bash
   cp .env.example .env.local
   # 编辑 .env.local 文件，填入你的 API 密钥
   ```

3. **设置数据库**
   - 在 Supabase 中创建表（详见 [docs/setup.md](./docs/setup.md)）

4. **启动开发服务器**
   ```bash
   pnpm dev
   ```

5. **访问应用**
   - 用户端：http://localhost:3000
   - 管理端：http://localhost:3000/admin/dashboard

## 项目结构

```
app/
├── (user)/                    # 用户端路由组
│   ├── layout.tsx             # 用户端布局
│   ├── page.tsx               # 首页 - 图片生成
│   ├── img-list/
│   │   └── page.tsx           # 公开图片列表
│   └── my/
│       └── page.tsx           # 个人控制台
├── (admin)/                   # 管理端路由组
│   ├── layout.tsx             # 管理端布局
│   └── dashboard/
│       └── page.tsx           # 仪表盘
├── (auth)/                    # 认证页面
│   ├── login/
│   │   └── page.tsx
│   └── register/
│       └── page.tsx
└── api/                       # API 路由
    └── generate/              # 图片生成 API

components/
├── ui/                        # shadcn/ui 组件
└── user/                      # 用户端组件

lib/
├── services/                   # 服务抽象接口
├── providers/                  # 具体实现
│   ├── supabase/             # Supabase 实现
│   └── ai/                   # AI 服务实现
└── supabase/                  # Supabase 客户端

docs/                           # 项目文档
└── setup.md                   # 详细配置指南
```

## 部署

### Vercel 部署

1. 将代码推送到 GitHub 仓库
2. 在 Vercel 中导入项目
3. 配置环境变量
4. 部署

### 自托管

```bash
# 构建项目
pnpm build

# 启动生产服务器
pnpm start
```

## 贡献

欢迎提交 Issue 和 Pull Request！

## 许可证

MIT
